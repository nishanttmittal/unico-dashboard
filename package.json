{
  "name": "unico-dashboard",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "UNICO Factory OS — CEO Dashboard (read-only command center over the live factory apps)",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist --dotfiles --nojekyll"
  },
  "dependencies": {
    "firebase": "^12.14.0",
    "react": "^19.2.6",
    "react-dom": "^19.2.6"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.3.0",
    "@vitejs/plugin-react": "^6.0.1",
    "gh-pages": "^6.3.0",
    "tailwindcss": "^4.3.0",
    "vite": "^8.0.12"
  }
}
